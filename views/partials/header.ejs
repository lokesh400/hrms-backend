  <nav class="bg-white border-b border-gray-200 shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16 items-center">
        <div class="flex items-center space-x-4">
          <span class="text-lg font-bold text-blue-600">HRMS</span>

          <% if (currentUser) { %>
            <a href="/dashboard" class="text-gray-700 hover:text-blue-600 transition">Dashboard</a>
            <a href="/attendance/today" class="text-gray-700 hover:text-blue-600 transition">My Attendance</a>

            <% if (currentUser.role === "admin" || currentUser.role === "hr") { %>
              <a href="/attendance/all" class="text-gray-700 hover:text-blue-600 transition">All Records</a>
              <a href="/employee" class="text-gray-700 hover:text-blue-600 transition">Employees</a>
               <a href="/admin/leave" class="text-gray-700 hover:text-blue-600 transition">Leave</a>
            <% } %>

            <a href="/logout" class="text-red-600 hover:text-red-800 font-medium ml-4">Logout</a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>
